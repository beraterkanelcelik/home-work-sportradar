# Django
DJANGO_SECRET_KEY=django-insecure-change-this-in-production
DEBUG=False

# PostgreSQL
DB_NAME=ai_agents_db
DB_USER=app
DB_PASSWORD=app
DB_HOST=db
DB_PORT=5432

# Langfuse
LANGFUSE_ENABLED=true
LANGFUSE_BASE_URL=http://langfuse-web:3000
LANGFUSE_HOST=http://langfuse-web:3000

# Langfuse Secrets (empty but valid)
SALT=""
ENCRYPTION_KEY=7407c305338d0b48403c291f1ac844b3f87cad9c7553e1f73240de28c2aa5171
NEXTAUTH_SECRET=""
NEXTAUTH_URL=http://localhost:3001

# ClickHouse
CLICKHOUSE_USER=clickhouse
CLICKHOUSE_PASSWORD=clickhouse
CLICKHOUSE_CLUSTER_ENABLED=false

# Redis
REDIS_AUTH=myredissecret
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_MAX_CONNECTIONS=200

# MinIO
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123

# Temporal Worker Scaling (see docs/STRESS_TEST.md)
# Scale workers for higher load: docker-compose up --scale worker=4
# Or use deploy.replicas in docker-compose.yml (requires Compose v2)
# Max concurrent workflow tasks (default: 200)
TEMPORAL_MAX_CONCURRENT_WORKFLOWS=250
# Max concurrent activity tasks - controls how many LLM calls run simultaneously (default: 100)
TEMPORAL_MAX_CONCURRENT_ACTIVITIES=250
